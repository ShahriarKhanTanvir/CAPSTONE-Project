# Git Workflow Rules

- **Do not push anything to UAT (GitHub)** unless the user explicitly confirms they are happy with the staging area (local XAMPP). Always ask for permission before running a `git push` command to the remote repository.
